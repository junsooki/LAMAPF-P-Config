# docs/

Documentation for every code file in `src/` and `tests/`.

- `cpp/`: one-to-one docs for C++ files
- `py/`: one-to-one docs for Python files
- `tests/`: one-to-one docs for test files
- `design/`: planning model design notes
