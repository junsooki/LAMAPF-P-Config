# docs/design/

Design notes for planning models and flow constructions that are not tied to a single code file.

- `event_driven_two_layer.md`: event-driven one-round planning with Empty/Loaded layers.
- `sync_two_stage_model.md`: synchronized two-stage batch model (pickup stage then drop stage).
